using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using Yarn.Unity;

public class YarnCommands : MonoBehaviour
{
    //note: this is commented out because I am too lazy to add the correct version of YarnSpinner to this project right now :) 
    /*
    [YarnCommand("initialize_line")]
    static void SetSpeakerInfo(string info, string info2)
    {
        string speaker = info;
        //If not specified emotion, set to neutral
        string emotion = info2;
        DialogueAudio.SetDialogueAudio(speaker, emotion);
        DialogueSpawner.InitializeDialogueBox(speaker);
        DialoguePortrait.Instance.SetActivePortrait(info, info2);
    } */
}
